# Termux Setup

Termux support is an unverified local CLI target. Do not hard-code Android paths: run the CLI from the desired project root and optionally select `--file`. Install a compatible Node.js runtime through Termux's approved package workflow, then run the README commands. The `TermuxExecutionEnvironment` is an identifier/policy placeholder and has not been tested on Android.

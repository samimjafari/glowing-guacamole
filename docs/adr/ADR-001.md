# ADR-001: Use an SDK-independent prompt boundary

**Status:** C

## Decision
Prompt preprocessing logic is reusable, but native registration is deferred until installed declarations are inspected.

## Consequence
Documentation and code must preserve this evidence level; no stronger operational claim is permitted without target-environment proof.

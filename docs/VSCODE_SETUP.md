# VS Code Setup

No VS Code extension is included. A future extension may call the same local CLI or spawn the stdio protocol server; it must not create a loopback/network bridge. `workspaceDiagnostics` is a library helper, not a registered VS Code command. VS Code integration status remains UNPROVEN pending an extension entry point and a real workspace test.
